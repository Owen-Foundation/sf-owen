diff --git a/src/evaluate.cpp b/src/evaluate.cpp
index 7ec314f..905b320 100644
--- a/src/evaluate.cpp
+++ b/src/evaluate.cpp
@@ -59,6 +59,15 @@ Value Eval::evaluate(const Eval::NNUE::Network&     network,
     int material = 534 * pos.count<PAWN>() + pos.non_pawn_material();
     int v        = nnue + (nnue * i64(material) + optimism * i64(7675)) / 91000;
 
+    // Owen Dynamic Anti-Trade & Queen Retention Bonus
+    if (pos.count<QUEEN>(WHITE) > 0 && pos.count<QUEEN>(BLACK) > 0)
+    {
+        if (v > 0)
+            v += 14;
+        else if (v < 0)
+            v -= 14;
+    }
+
     // Damp down the evaluation linearly when shuffling
     v -= v * pos.rule50_count() / 199;
 
